import 'package:flutter/material.dart';
import 'Telas de usuário/login.dart';

// Método principal da aplicação
void main() {
  // ignore: prefer_const_constructors
  runApp(MaterialApp(
    home: const Login(),
    debugShowCheckedModeBanner: false,
  ));
}
